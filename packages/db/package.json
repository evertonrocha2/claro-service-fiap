{
  "name": "@sync/db",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "migrate": "prisma migrate dev",
    "seed": "tsx prisma/seed.ts",
    "generate": "prisma generate"
  },
  "type": "module",
  "types": "./src/index.ts",
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "tsx": "^4.19.0"
  }
}
